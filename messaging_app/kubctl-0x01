#!/bin/bash
# Scale Django app deployment to 3 replicas
kubectl scale deployment messaging-app --replicas=3

# Verify pods
kubectl get pods

# Monitor resource usage
kubectl top pods

# Run load testing with wrk (adjust duration/threads/connections as needed)
wrk -t2 -c50 -d30s http://$(minikube ip):8000/
